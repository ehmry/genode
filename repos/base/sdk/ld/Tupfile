include_rules
include $(SDK_ABI_INCLUDE)

SED_FLAGS += -e 's|@prefix@|$(SDK_PREFIX)|'
SED_FLAGS += -e 's|@libdir@|$(SDK_LIB_PREFIX)|'
SED_FLAGS += -e 's|@includedir@|$(SDK_INCLUDE_PREFIX)|'

: *.pc.in |> !sed |> pkgconfig/%d.pc
include $(SDK_TAR_INCLUDE)
