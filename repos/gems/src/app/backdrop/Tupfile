TARGET_NAME = backdrop
LIBS += libpng libc

include_rules

LIBS += blit

: foreach *.cc $(REP_DIR)/src/lib/file/*.cc |> !cxx |> %B.o {obj}
: {obj} |> !prg |> {bin}
: {bin} |> !collect_bin |>
include &(BIN_RULES)
