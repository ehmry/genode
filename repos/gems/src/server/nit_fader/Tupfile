TARGET_NAME = nit_fader
include_rules

INCLUDES += -I.
INCLUDES += -I$(REP_DIR)/../demo/include
LIBS += blit

: foreach *.cc |> !cxx |> %B.o {obj}
: {obj} |> !prg |> {bin}
: {bin} |> !collect_bin |>
include &(BIN_RULES)
