TARGET_NAME = ping
INCLUDES += -I.
include_rules

: foreach *.cc |> !cxx |> %B.o {obj}
: foreach ../../lib/net/*.cc |> !cxx |> %B.o {obj}

: {obj} |> !prg |> {bin}
: {bin} |> !collect_bin |>
include &(BIN_RULES)
