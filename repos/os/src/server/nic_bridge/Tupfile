TARGET_NAME = nic_bridge
INCLUDES += -I.
include_rules

: foreach *.cc |> !cxx |> %B.o {obj}
: foreach ../../lib/net/*.cc |> !cxx |> %B.o {obj}

: {obj} |> !prg |> {bin}
: {bin} |> !collect_bin |>
include &(BIN_RULES)
