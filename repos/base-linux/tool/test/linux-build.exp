# Put derivation variables in scope.
set system_image $env(systemImage)

puts $user_script "set system_image $system_image"
puts $user_script "source $platform_setup"
puts $user_script $test_script

close $user_script

# Link the system image for later reference.
exec ln -s $system_image $out/image
